# Sidebar audit — 2026-09-17

Scope: current sidebar implementation and its application wiring. The menu structure was added afterward; no unrelated sidebar fixes were applied.

1. **HIGH — R-26/R-35:** `src/components/app-sidebar.tsx:13-25` opens a workspace menu, but selecting either item has no state update or visible result. Wire a selected workspace action, or remove the menu until workspaces are real.

2. **HIGH — R-26:** `src/components/app-sidebar.tsx:50-52` renders `Username` as a button with no action. Make it a real account menu/link, or render it as non-interactive text.

3. **RESOLVED — build blocker:** `src/main.tsx:9` mounts `<Layout />` without page content. `src/layout.tsx:4` now makes `children` optional so the shell can build without inventing page content.

4. **MEDIUM — R-38/C-3:** `src/components/app-sidebar.tsx:30-45` contains empty `Application` and `Help` groups plus an empty `SidebarGroup`. The rendered sidebar is mostly blank and offers no destinations. Add real items, show an honest placeholder such as “Coming soon”, or delete the sections.

5. **MEDIUM — R-35:** `src/components/ui/sidebar.tsx:74-86` writes `sidebar_state` but never reads it. The claimed persisted open/closed preference is lost on reload; either hydrate from the cookie or remove the cookie write.

6. **MEDIUM — verification blocker:** `npm run lint` fails in `src/hooks/use-mobile.ts:14`, `src/components/ui/button.tsx:57`, and `src/components/ui/sidebar.tsx:722`. The first triggers `set-state-in-effect`; the latter two export non-components from component modules. Resolve these errors before treating the sidebar as shippable.

7. **LOW — ponytail:** `src/components/app-sidebar.tsx:10,45` has an empty header and empty group. Delete both; they add structure without behavior.

8. **LOW — ponytail:** `src/components/ui/sidebar.tsx:417-696` ships many unused sidebar variants (actions, badges, skeletons, submenus, input, inset) for one current sidebar. Keep the shared primitive only if a second use is imminent; otherwise remove the unused API.

## Verification

- `npm run build`: PASS.
- `npm run lint`: FAIL — three errors, including the mobile hook and two Fast Refresh export warnings.
- `git diff --check`: PASS.
- Desktop preview: workspace menu opens; workspace selection closes the menu but does not change the selection; Help collapses/expands; sidebar collapse works.
- Keyboard: sidebar controls are reachable in the accessibility tree.
